services:
  - type: web
    name: rag-api
    env: python
    plan: free
    pythonVersion: "3.11"  
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: uvicorn api_prod:app --host 0.0.0.0 --port $PORT